<section id = "show_section">

  <h1 class="table_link">
    <%= @driver.name%></br></br>
  </h1>

  <%= @driver.name%> (vin: <%= @driver.vin%>)

  <P class="header">
    Trip Average : <%= @driver.average_rating %>
  </P>

  <table class="listing" summary="Riders Trip List">

    <tr class="header">
      <th>Name</th>
      <th>Date</th>
      <th>Rating</th>
      <th>Show Trip</th>

    </tr>

    <% @driver.trips.each do |trip| %>
    <tr>
      <td><%= trip.driver[:name] %></td>
      <td><%= trip[:date] %></td>
        <td><%= trip[:rating] %></td>
        <td>
          <%= link_to "Show", trip_path(trip), class: "button_link" %>
        </td>






    </tr>
    <% end %>
  </table></br></br></br>














  <!-- <p>
    <%= link_to "Back", drivers_path%>
    <%= link_to "Edit", edit_driver_path%>
    <%= link_to "Delete", driver_path,
    data: {method: :delete, confirm: "Are you sure you want to delete #{@driver.name} from the list?"}%>
  </p>

  <h3>
    List of <%= @driver.name%>'s passengers
  </h3>


  <!-- TABLE -->

  <!-- <div class="tbl-header"> -->
  <!-- <div> -->
    <!-- <table cellpadding="0" cellspacing="0" border="0"> -->
      <!-- <thead> -->

        <!-- <tr>
          <th>Name</th>
          <th>Date</th>
          <th>Rating</th>
          <th>Show Trip</th>

        </tr>
      </thead>
    </table>
  </div>
  <div class="tbl-content"> -->
  <!-- <div> -->
    <!-- <table cellpadding="0" cellspacing="0" border="0">
      <% @driver.trips.each do |trip| %>
      <tr>
        <td><%= trip.rider[:name] %></td>
        <td><%= trip[:date] %></td>
        <td><%= trip[:rating] %></td>
        <td>
          <%= link_to "Show", trip_path(trip), class: "button_link" %>
        </td>

      </tr>
      <% end %>

    </table>
  </div>

</section> -->
