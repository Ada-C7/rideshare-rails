<h1>The driver you selected!</h1>

Driver name: <%= @result_driver.name %>

VIN: <%= @result_driver.vin %>

Average rating: <%= @result_driver.find_avg_rating.round(1) %>

<table>
  <tr>
    <th>Trip ID</th>
    <th>Driver ID</th>
    <th>Passenger ID</th>
    <th>Date</th>
    <th>Rating</th>
  </tr>
  <% @result_driver.trips.each do |trip| %>
  <tr>
    <td><%= link_to trip.id, trip_path(trip.id) %></td>
    <td><%= link_to trip.driver.id, driver_path(trip.driver_id) %></td>
    <td><%= link_to trip.passenger.name, passenger_path(trip.passenger_id) %></td>
    <td><%= trip.date %></td>
    <td><%= trip.rating %></td>
  </tr>
  <% end %>
</table>



 <p>ALSO NEED TO DISPLAY: total earnings
 (each trip -15%)</p>

 <%= link_to "Delete", driver_path(@result_driver.id),
   data: {confirm: "Do you want to delete this driver? NO GOING BACK!!!"},
   method: :delete
 %>
<%= link_to "Edit", edit_driver_path %>
