<ul>
  <% @driver.errors.each do |field, msg| %>
    <li>
      <%= field %> <%= msg %>
    </li>
    <% end %>
</ul>

<%= form_for @driver do |f| %>

     <%= f.label :name %>
     <%= f.text_field :name %>

     <%= f.label :vin %>
     <%= f.text_field :vin %>

     <%= f.label :status %>
     <%= f.select :status, [["Available", true], ["Not Available", false]], prompt: "Please Select" %>

     <%= f.submit %>

<% end %>
