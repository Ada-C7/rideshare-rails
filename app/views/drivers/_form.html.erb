<%= form_for @driver, method: form_method do |f| %>

<%= f.label :name, class: "driver" %></br>
<%= f.text_field :name %></br>

<%= f.label :vin %></br>
<%= f.text_field :vin %></br>

<%= f.submit action_name, method: :put %>

<% end %>


<%= link_to "Back to Driver List", drivers_path %>
