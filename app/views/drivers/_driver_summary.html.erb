
  <table style=" width:80%" >

     <% @drivers.each do |item| %>
        <tr>
            <td> <%= item.id %> </td>
            <td> <%=link_to "#{item.name}",driver_path(item.id)%></td>
            <td> <%= item.vin  %> </td>
        </tr>
     <% end %>
