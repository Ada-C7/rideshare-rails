<h1>Drivers#index</h1>
<main>
  <ul>
    <% @drivers.each do |driver| %>
      <li><%= link_to driver[:name].capitalize, driver_path(driver.id) %></li>
    <% end %>
  </ul>
</main>
<footer>
</footer>
