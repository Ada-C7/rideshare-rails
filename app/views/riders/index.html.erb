<article class="index">

  <h1>All Riders</h1>

  <div class="scroll">
    <ul>
        <% @riders.each do |rider| %>
          <li>
            <%= link_to rider.name, rider_path(rider.id) %>
          </li>
        <% end %>
    </ul>
  </div>

</article>
