<h1> List of all riders: </h1>
<table>
  <tr>
    <th>Id</th>
    <th>Name</th>
    <th>Phone number</th>
  </tr>

  <%@riders.each do |rider|%>
    <tr>
      <td><%=rider.id %></td>
      <td><%=link_to rider.name, rider_path(rider.id) %></td>
      <td> <%=rider.phone_num %></td>
    </tr>
  <%end%>
</table>
