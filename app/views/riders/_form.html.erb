<%= form_for @rider, method: form_method do |f| %>

<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :phone_num, "Phone Number" %>
<%= f.text_field :phone_num %>

<%= f.submit "Save" %>

<% end %>
