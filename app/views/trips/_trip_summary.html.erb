<%= link_to trip.name, trip_path(trip.id) %>
<%= link_to "Edit", edit_trip_path(trip), method: :get %>
<%= link_to "Delete", trip_path(trip), method: :delete, data: {confirm: "Are you sure you want to delete Trip #{trip.id}?"} %>
