<h1>Trip Details</h1>

<ul>
  <li>Cost: <%= @result_trip.cost %></li>
  <li>Rating: <%= @result_trip.rating %></li>
  <li>Driver: <%= @result_trip.driver.name %></li>
  <li>passenger: <%= @result_trip.passenger.name %></li>
</ul>

<h2><%= link_to "Edit", edit_trip_path(@result_trip.id)%></h2>
<h2><%= link_to "Delete", trip_path(@result_trip.id), data: { confirm: "Are you sure you want to delete this trip?" }, method: :delete %></h2>
