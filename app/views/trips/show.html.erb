<h1>The trip you selected!</h1>

<%= @result_trip.driver_id %>

<%= @result_trip.passenger_id %>

<%= @result_trip.date %>

<%= @result_trip.rating %>

<%= @result_trip.cost %>

<p>
  update rating and other details, link to driver and passenger
</p>

<%= link_to "Delete", trip_path(@result_trip.id),
  data: {confirm: "Do you want to delete this trip? NO GOING BACK!!!"},
  method: :delete
%>
