<h1> Name: <%= @trip.date %> </h1>
<h2> ID: <%= @trip.id %> </h2>

<%= link_to "Edit", edit_rider_trip_path(@trip.id) %>
<%= link_to "Delete", rider_trip_path(@trip), method: :delete,
    data: {confirm: "Are you sure"} %>
