<article class="show">
<h1>All the Trips Details</h1>

<%= @result_trip.driver.name %>
<%= @result_trip.rider.name %>
<%= @result_trip.rating %>
<%= @result_trip.date %>

<%= link_to "Delete", trip_path(@result_trip.id),
  data: { confirm: "Are you sure you want to delete this trip?" },
  method: :delete
%>
</article>
