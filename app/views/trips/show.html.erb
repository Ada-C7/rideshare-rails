<h2>Trip # <%= @trip[:id] %></h2>
<h3>Driver: <%= link_to "#{@trip[:driver_id]}", driver_path(@trip.driver_id) %></h3>
<h3>Passenger: <%= link_to "#{@trip[:passenger_id]}", passenger_path(@trip.passenger_id) %></h3>
<h3>Rating: <%= @trip[:rating] %> <br> Date: <%= @trip[:date] %> <br> Price: $<%= @trip[:price] %></h3>
