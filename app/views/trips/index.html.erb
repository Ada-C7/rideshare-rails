<% @trip_content = true %>

<% content_for :trips do %>
  <section class="yield">
    <h1 class="title">Trips</h1>
    <ul class="list">
       <% @trips.each do |trip| %>
          <li> <%= link_to "Trip #{trip.id}", trip_path(trip.id) %> </li>
       <% end %>
    </ul>
  </section>
<% end %>
