<!-- <% if @rider.errors.messages.length > 0 %>
<section class="errors">
  <h2> Something wrong! </h2>
  <ul>
    <% @rider.errors.messages.each do |field, problem_list| %>
    <% problem_list.each do |problem| %>
    <li>
      <%= field %>:
      <%= problem %>
    </li>
    <% end %>
    <% end %>
  </ul>
</section>
<% end %> -->



<%= form_for @trip do |f|%>
<%= f.label :date %>
<%= f.text_field :date %>

<%= f.submit %>
<% end %>
