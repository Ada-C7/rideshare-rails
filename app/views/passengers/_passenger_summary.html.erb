<link href="https://fonts.googleapis.com/css?family=Dosis:700" rel="stylesheet">

<%= link_to passenger.name, passenger_path(passenger.id) %>
<%= link_to "Edit", edit_passenger_path(passenger), method: :get %>
<%= link_to "Delete", passenger_path(passenger), method: :delete,
      data: {confirm: "Are you sure you want to delete Passenger #{passenger.name}?"} %>
