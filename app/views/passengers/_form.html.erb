<%= form_for @result_passenger do |f| %>

<%= f.label :name, class: "passenger" %></br>
<%= f.text_field :name %></br>

<%= f.label :phone_num %></br>
<%= f.text_field :phone_num %></br>

<%#= f.submit "Create" %>
<%= f.submit action_name, method: :put %>

<%#= button_to "Home", passengers_path, method: :get %>

<!-- <%= button_to 'Delete', delete_passengers_path(:id), method: :delete, data: { confirm: 'Are you sure?' } %> -->

<% end %>
<%= link_to "Back to Passenger List", passengers_path %>
