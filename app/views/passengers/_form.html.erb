<section>
  <%= form_for @passenger do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :phone_num %>
    <%= f.label :phone_num %>

    <%= f.submit value: "#{submit_text}" %>
  <% end %>
</section>
