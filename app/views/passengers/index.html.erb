<link href="https://fonts.googleapis.com/css?family=Dosis:700" rel="stylesheet">

<h2>Passengers</h2>
<h3><%= link_to "Add a new passenger", new_passenger_path %></h3>


<ul>
  <% @passengers.each do |passenger| %>
    <li>
      <%= render partial: "passenger_summary", locals: {passenger: passenger} %>
    </li>
  <% end %>
</ul>
