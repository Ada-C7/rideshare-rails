<h1>Here are the rideshare passengers...</h1>

<%= button_to "Add an new passenger", new_passenger_path, method: :get %>

<ol>
  <% @passengers.each do |passenger| %>

  <li> <%= link_to passenger.name, passenger_path(passenger)%> </li>
  <% end%>

</ol>
