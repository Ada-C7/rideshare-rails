<%= render "shared/menu" %>

<h1>Passenger List</h1>

<h2>Click on a Passenger for Info:</h2>

  <ul>

    <% @passengers.each do |passenger| %>
    <%= link_to passenger[:name], passenger_path(passenger.id) %>
      <li>
        <%= passenger[:description] %>
      </li>
    <% end %>
  </ul>
