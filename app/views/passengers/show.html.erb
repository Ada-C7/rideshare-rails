<h1>Passenger Details</h1>
<ul>
  <li>Name: <%= @result_passenger.name %></li>
  <li>Phone number: <%= @result_passenger.phone_num %></li>
  <li>Total Spent: <%= @result_passenger.total_cost %></li>
</ul>

<% @result_passenger.trips.each do |trip| %>
<ul>
  <li>Trip ID: <%= link_to trip.id, trip_path(trip.id) %>
  Driver: <%= link_to trip.driver.name, driver_path(trip.driver.id) %>
  Cost: $ <%= (trip.cost / 100.0).round(2) %>
  Rating: <%= trip.rating %>
  <%= button_to "Update Rating", rating_trips_path(trip.id), method: :patch %>
  </li>
</ul>
<% end %>


<h2><%= link_to "All Passengers", passengers_path %></h2>
<h2><%= link_to "Edit", edit_passenger_path(@result_passenger.id)%></h2>
<h2><%= link_to "Delete", passenger_path(@result_passenger.id), data: { confirm: "Are you sure you want to delete this passenger?" }, method: :delete %></h2>

<h2><%= link_to "Make New Trip", new_trip_path(@result_passenger.id), method: :post %></h2>
<%= warning %>
